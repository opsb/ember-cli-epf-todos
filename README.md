# Todos with ember cli and epf 

## Setup

    $ cd server
    $ bundle

    $ cd client
    $ npm install
    $ bower install

## Running

    $ cd server
    $ rails 

    $ cd client
    $ ember serve
